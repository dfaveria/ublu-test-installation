Files needed (in a secret as base64 encoded strings) for TLS ingress.

- Must be named tls.crt and tls.key, respectively
- if you have a crt file and a certificate chain (ca-bundle) append your .ca-bundle to your .crt cert
